{
  "version": 2,
  "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm run build:client",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "outputDirectory": "dist/spa",
  "framework": null,
  "functions": {
    "api/index.py": {
      "runtime": "python3.11",
      "includeFiles": "fastapi_server/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
